# minimal.yaml defines a minimal deployment for use in testing or demo
# deployments.
applications:
  haproxy:
    num_units: 1
    constraints: cores=1
  jimm:
    num_units: 1
    constraints: arch=amd64 cores=1
  postgresql:
    num_units: 1
    constraints: cores=1
